# TODO

- [x] 네이버 API 최신 응답 키와 기존 매핑 차이 확인
- [x] `app.py`의 `KEY_MAP`에 구키/신키 호환 매핑 추가
- [x] 동일 표시 컬럼으로 합쳐지는 중복 컬럼 정리 로직 보강
- [x] `load_stock_data` 후처리 방어 코드 적용 및 출력 컬럼 검증
- [x] 검증 결과 기록

## Review

- `KEY_MAP`에 네이버 최신 응답 키와 기존 키를 함께 매핑해 구버전/신버전 응답을 모두 수용하도록 조정함
- 동일 한글 컬럼명으로 합쳐지는 경우 첫 유효값으로 병합해 중복 컬럼으로 인한 후처리 오류를 방지함
- 검증: `python3 -m py_compile app.py` 통과
- 검증: 실제 네이버 API 샘플 응답 기준 `현재가(원)`, `등락률`, `배당수익률` 등 핵심 최종 컬럼 복구 확인
- 검증: `priority_cols`와 `format_dict` 기준 최종 표시 컬럼 정합성 확인
- 테스트: 미실행 (사유: 저장소에 표준 테스트 스크립트 없음, 로컬 Python 환경에 앱 의존성 미설치)
